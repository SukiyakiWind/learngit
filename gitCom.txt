Hello World!
ez miracle.
git add file.name
git commit -m 'change text'
git status
git diff
cd
pwd
rm filename.xxx
git rm filename.xxx

$ git branch
$ git checkout -b <name> === $ git branch <name> $ git checkout <name> === git switch -c <name>
$ git checkout -d <name>
$ git merge <name>
$ git switch <name>